{
  "name": "@oneplatform/example-saml-auth-provider",
  "version": "1.0.0",
  "private": true,
  "description": "Example OnePlatform plugin: SAML 2.0 authentication provider",
  "type": "module",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc",
    "test": "vitest run",
    "test:watch": "vitest",
    "lint": "eslint src/",
    "typecheck": "tsc --noEmit",
    "validate": "op plugin validate",
    "pack": "op plugin pack",
    "clean": "rm -rf dist"
  },
  "dependencies": {
    "@oneplatform/plugin-sdk": "^0.1.0"
  },
  "devDependencies": {
    "typescript": "^5.5.0",
    "vitest": "^2.0.0",
    "eslint": "^9.0.0"
  },
  "engines": {
    "node": ">=18.0.0"
  },
  "files": [
    "dist",
    "manifest.json"
  ]
}
